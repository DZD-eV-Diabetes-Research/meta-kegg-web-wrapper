# Run tests for mekeweserver

API Tests: in development. not any automation yet

## options

env vars:
    * MEKEWE_TESTS_RESET_DB -> deletes the db at start of the tests and creates a new one.


This